using System.Collections.Generic;
using DataLayer;

namespace MiddleServer
{
    public class MiddleLayer
    {
        /* create a dynamic array for each different data entity */
        private List<Product> allProducts = new List<Product>();
        private List<Salesperson> allSalespeople = new List<Salesperson>();
        private List<Customer> allCustomers = new List<Customer>();
        private List<Sales> allSales = new List<Sales>();
        private List<Discount> allDiscount = new List<Discount>();
            
    }

}/* method create a new product */
